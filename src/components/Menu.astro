---
import { getCollection } from "astro:content";
import Card from "./Card.astro";
const drinks = await getCollection("drinks");
---

<section id="Menu">
  <ul>
    {drinks.map((drink) => <Card drink={drink} />)}
  </ul>
</section>
